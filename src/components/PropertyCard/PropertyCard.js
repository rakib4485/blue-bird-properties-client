import React from 'react';

const PropertyCard = ({property}) => {
    const {img, title, location, price, size} = property;
    return (
        <div>
            
        </div>
    );
};

export default PropertyCard;